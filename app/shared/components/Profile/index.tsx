"use client";

import { Box, Paper } from "@mui/material";
import { useAuth } from "../../hooks/useAuth";

export function Profile() {
  return <Box component={Paper} width={600} height={800}></Box>;
}
